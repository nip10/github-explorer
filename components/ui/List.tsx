import styled from "styled-components";

const UnstlyedList = styled.ul`
  padding: 0;
  list-style-type: none;
`;

const UnstyledListItem = styled.li`
  text-decoration: none;
`;

export { UnstlyedList, UnstyledListItem };
